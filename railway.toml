[build]
builder = "NIXPACKS"
buildCommand = "npm run build"

[deploy]
startCommand = "mkdir -p /data/uploads /data/db && cp -rn public/uploads/* /data/uploads/ 2>/dev/null || true && npx prisma migrate deploy && npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
