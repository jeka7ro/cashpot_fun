[build]
builder = "NIXPACKS"
buildCommand = "npm run build"

[deploy]
startCommand = "mkdir -p /data/uploads /data/db && rm -rf public/uploads && ln -s /data/uploads public/uploads && npx prisma migrate deploy && npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
